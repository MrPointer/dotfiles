# Managed by chezmoi. Trusts each active git identity signed by any available local key.
{{ $emails := list .personal.email -}}
{{ if and .personal.work_env .personal.work_email -}}
{{ $emails = append $emails .personal.work_email -}}
{{ end -}}
{{ range $email := $emails -}}
{{ range $name := (list "id_rsa.pub" "id_ed25519.pub") -}}
{{ $path := joinPath $.chezmoi.homeDir ".ssh" $name -}}
{{ if stat $path -}}
{{ $email }} {{ output "cat" $path | trim }}
{{ end -}}
{{ end -}}
{{ end -}}
